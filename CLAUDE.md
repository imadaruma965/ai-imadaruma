# CLAUDE.md（開発憲法）

Claude Code / Cursor / AIエージェントが `ai-imadaruma` で作業する際の **最上位の作業憲法**。本ファイルは毎ターン常駐するため、**要点だけ**を置く。組織の詳細は `01_内閣府/AI主管定義.md`（AI主管の唯一の正本）を、必要な節だけ読む。

---

## 0. 作業効率ルール（全作業共通・最優先で守る）

1. **人格（Skill）は、その人格の判断や語り口が価値を生むときだけ起動する。** ファイル操作・要約・整理・簡単な調べ物・簡易な指示は、Skillを起動せず直接やる
2. **読む前に絞る。** `grep -n '^#' ファイル` で見出しを見て、該当節だけ Read（offset/limit）する。10KBを超えるファイルを全文読まない（そのファイルを編集する場合を除く）
3. **探すときはパスを限定する。** Vault（`05_情報省/ダヴィンチ図書館/`）はMCP `imada-knowledge` 経由（手順：`.claude/skills/_shared/knowledge_growth.md`）。`03_貿易省/産業庁/商品開発/ページ撮りPDF/`（venv・画像）は検索しない
4. **ログや記録への追記は、ファイルを読まずに末尾へ足す**（`cat >> パス <<'EOF'`）
5. 同じファイルを1会話で読み直さない。報告は結論から短く
6. 新しいファイルを作る前に、既存の正本へ追記できないか確かめる（ファイルを分散させない）

**会話の冒頭**：`awk '/^## 未承認/,/^## 振り分け不明/' 05_情報省/学習ループ/反映待ち.md` を1回だけ実行し、行があれば1行でBOSSに伝える。BOSSが「反映して」と言ったら適用する。前回の続きのときだけ `01_内閣府/申し送り/` の最新1件、学習ダイジェストは求められたときだけ読む。コンテキストが逼迫したら `01_内閣府/申し送り/YYYY-MM-DD_<件名>.md` を残して新セッションへ。

---

## 1. 目的と最高法規

- `ai-imadaruma` は、自己統治論・仁子思想・じぶん憲法・四柱統治フレームワーク・明君七徳をもとに、**11月末までにサッカー以外の事業で月間総合売上50万円以上**を達成するための国家OS
- BOSS：いまだ唯仁／統治原理：仁子思想／**最高法規：じぶん憲法 v3**。すべての人格・戦略・日次運用は憲法の下位。矛盾したら憲法を優先する

---

## 2. 組織と振り分け（概要。正本は `01_内閣府/AI主管定義.md`）

国家は二系統：【法】**律令府**（律政省＝韓非子＋栄一、対等並列。BOSS直属）／【執行】**内閣府**（執行官スマラビが四柱＝修身省・貿易省・理財省・情報省を一任）。

| 依頼の中身 | 担当（所属） | Skill |
|---|---|---|
| 朝会議・今日の計画・進捗・振り分け | 賢いうさぎ／スマラビ（内閣府・執行官） | `/smart_rabbit` |
| 戦略・価格・ロードマップの義利監査（一時停止勧告権） | 栄一（律政省） | `/eiichi` |
| 規約・契約・自動化のリスク点検 | 韓非子（律政省） | `/kanpishi` |
| 七徳の教育・検証、内省 | 仁子（修身省） | `/jinshi` |
| 睡眠・食事・習慣、日次タスク分解 | 尊徳（修身省） | `/sontoku` |
| 営業・応募・交渉・提携、製品づくりの統括 | 坂本龍馬（貿易省・統括） | `/sakamoto_ryoma` |
| 市場・競合・案件のリサーチ | ルパン（貿易省） | `/lupin` |
| 自己統治論の思想研究 | アショーカ（貿易省） | `/ashoka` |
| 技術設計・自動化・Skill実装パッチ | テスラ（貿易省・産業庁） | `/tesla` |
| ビジュアル・画像プロンプト | 北斎（貿易省・文化庁） | `/hokusai` |
| 発信文章（LinkedIn／SNS／Kindle／note） | 正篤＝まさひろ（貿易省・文化庁） | `/masahiro` |
| 収入・支出・請求漏れ | ルカ（理財省） | `/luca` |
| ナレッジ統括・図書館・横断分析 | ダ・ヴィンチ（情報省） | `/davinci` |

混同禁止：仁子／尊徳・アショーカ／正篤は戦略を勝手に変えない（執行官・律政省へエスカレーション）。栄一・韓非子は点検で終え、実行まで抱え込まない。研究官はアショーカ、文筆官は正篤。雅称（修身院・勘定所・律令院・海援隊）は対外発信でのみ任意使用。通信は Claude Remote Control 一本（Cursorは実装時のみ）。

---

## 3. Git運用

- **作業前**：`git status`。clean でなければ作業を始めず、変更内容を報告する
- **作業後**：`git status` と `git diff --stat`。中身は必要なファイルに限って `git diff <path>` で確認する
- **コミット前**に報告：編集・作成したファイル／各ファイルの要点／旧表記チェック結果／Git状態／コミット可否。**ユーザーの明示指示なしにコミットしない**
- **push** はコミット後にユーザーが許可した場合のみ。push後は `git status` でリモートとの同期を報告する
- **Phase分離**：戦略変更・憲法変更・日次運用変更・ファイル整理を同一コミットに混ぜない。1コミット＝1目的

---

## 4. 禁止事項（正式名称としての復活禁止ほか）

- AI孫子・AI蕭何を正式名称として、軍師・相国・統治総理AI・「総理」（スマラビの肩書き。正式は「執行官」）を正式役職として復活させない
- `05_情報省/ダヴィンチ図書館/90_国家OS保管/legacy/` と `cabinet/chief_statecraft_minister.md` を勝手に編集しない
- 正本DOCX・旧版ファイルを勝手に削除しない
- 四柱そのもの（`00_律令府/憲法/四柱統治フレームワーク.md`）にAI主管を直接書かない。AI主管定義を複数ファイルで再定義しない
- ロードマップの基本戦略を勝手に変更しない。収益源を勝手に増やしすぎない

変更履歴・移行注記など文脈が明確な旧表記、補助原理としての単体表記 `孫子`／`蕭何` は残してよい。関連ファイルを編集したら、変更したファイルに限って旧表記チェックを行う：

```bash
grep -n "統治総理AI\|AI孫子\|AI蕭何\|軍師\|相国" <変更したファイル>
```

---

## 5. ディレクトリ

リポジトリルートは**国家**、各省庁が部屋、人格正本がその住人。

```text
00_律令府/   法の系統（律政省／憲法／理念／戦略・対話ログ）。索引は 00_律令府/README.md
01_内閣府/   執務室（賢いうさぎ・AI主管定義・申し送り）
02_修身省/   仁子・尊徳 ＋ 日次運用（今日・財政ボード・成長ボード・対話ログ）
03_貿易省/   坂本龍馬・ルパン・アショーカ ＋ 産業庁（テスラ・任務盤）・文化庁（北斎・正篤）
04_理財省/   ルカ
05_情報省/   ダ・ヴィンチ ＋ ダヴィンチ図書館（Vault。~/imada-knowledge と MCP はここを指す）＋ 学習ループ
99_研究院/   独立研究
.claude/skills/  人格Skill（起動時の読込はSKILL.mdが唯一の正）＋ _shared/knowledge_growth.md（MCP手順）
```

---

## 6. 次フェーズ予定（本作業では中身を作らない）

- Roadmap review and strategy refinement
- Habit and character education design
- Skills folder design（着手済：`_shared/knowledge_growth.md`＋`エージェント成長OS.md`。週次昇格は学習ループで自動化）
- Operational prompt templates
