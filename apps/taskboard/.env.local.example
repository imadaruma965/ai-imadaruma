# Cursor Dashboard → Integrations で発行したAPIキー
CURSOR_API_KEY="cursor_ここに自分のキー"

# 任意。未指定ならCursor側が利用可能モデルを選ぶ
SONTOKU_MODEL="auto"

# 任意。設定すると /api/state, /api/sontoku にヘッダ認証を要求する（LAN/Tailscale経由の第三者アクセス対策）
TASKBOARD_TOKEN=""
